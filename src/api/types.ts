export type Product = {
    id: string;
    name: string;
    storageId: string;
    category: string;
}

export type Storage = {
    id: string;
    name: string;
    roomId: string;
}

export type Room = {
    id: string;
    name: string;
    color: string;
}


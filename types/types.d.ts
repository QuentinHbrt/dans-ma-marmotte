declare module "@theme-ui/presets" {
  import { Theme } from "theme-ui";
  const presets: {
    base: Theme;
    system: Theme;
    funk: Theme;
    future: Theme;
    roboto: Theme;
    dark: Theme;
    deep: Theme;
    swiss: Theme;
    tosh: Theme;
    bootstrap: Theme;
    bulma: Theme;
    tailwind: Theme;
  };
  export = presets;
}
